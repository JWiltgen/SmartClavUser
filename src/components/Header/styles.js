import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    marginRight: 30,
    alignItems:"flex-end",
    justifyContent:"space-around",
  },
});
